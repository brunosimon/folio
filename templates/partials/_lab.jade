include ../mixins/_arrows_title.jade
include ../mixins/_project.jade

section.projects.lab
    +arrows_title("Lab")
    - var i = 0;
    each item in lab.items
        - i++
        - item.num = i < 10 ? "0" + i : i;
        +project(item)

