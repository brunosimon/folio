include ../mixins/_arrows_title.jade
include ../mixins/_project.jade

section.projects
    +arrows_title("Works")
    - var i = 0;
    each item in works.items
        - i++
        - item.num = i < 10 ? "0" + i : i;
        +project(item)

